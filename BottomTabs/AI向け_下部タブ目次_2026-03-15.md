# AI向け 下部タブ目次 (2026-03-15)

最終更新日: 2026-04-11

変更概要:
- `MainWindow.xaml` の下部タブ既定選択を `サムネイル進捗` と明示した
- `ThumbnailProgressTabPresenter` を追加し、進捗タブの活性監視、dirty 管理、可視時 flush を `MainWindow` から切り出した
- `ExtensionTabPresenter` を追加し、詳細タブの活性監視と dirty 管理を `MainWindow` から切り出した
- `ThumbnailErrorBottomTabHostController` を追加し、サムネ失敗タブの host 出し入れと layout 調整を `MainWindow` から切り出した
- `Log` タブを追加し、Debug限定でログ出力スイッチとログ末尾プレビューを分離した

## 目的

- `BottomTabs` 配下を、次に触る AI が最短で追えるようにする。
- `MainWindow` のどこを入口に見ればよいかを固定する。
- 各タブの責務境界を見失わないようにする。

## 最初に見る場所

1. `MainWindow.xaml`
   - 下部タブの host 配置を見る。
2. `MainWindow.xaml.cs`
   - 初期化線を見る。
3. `BottomTabs/Common`
   - 共通の選択制御と活性判定を見る。
4. 触る対象タブのフォルダ
   - partial と View を対で読む。
5. `Docs/forAI/比較メモ_UpperTabsCommon_BottomTabsCommon_役割対応_2026-04-01.md`
   - `UpperTabs/Common` と `BottomTabs/Common` の責務差を横断で確認する。
6. `Docs/forAI/比較メモ_UpperTabsCommon_BottomTabsCommon_ここに置かない物_2026-04-01.md`
   - `Common` に置かない物の禁止線を確認する。

## ディレクトリ目次

まずは役割の大枠だけ掴む。

- `BottomTabs/Common`
  - 上側メインタブ切替に引っ張られる共通処理、選択取得、活性判定の入口。
- `BottomTabs/Extension`
  - 下部 `詳細` タブ本体。表示切替、選択追従、詳細サムネ制御。
- `BottomTabs/Bookmark`
  - bookmark 一覧の読込、再読込、追加削除、defer。
- `BottomTabs/SavedSearch`
  - 保存済み検索条件タブの placeholder と dirty 管理。
- `BottomTabs/ThumbnailProgress`
  - 進捗 UI 本体、タイマー、表示中だけ反映する制御。
- `BottomTabs/LogTab`
  - Debug限定のログ制御タブ。カテゴリ別スイッチ、即時保存、ログ末尾プレビュー。
- `BottomTabs/DebugTab`
  - debug 表示制御、ログ末尾プレビュー、操作ボタン群。
- `BottomTabs/ThumbnailError`
  - サムネ失敗一覧、host 接続、可視監視、救済導線。

次に必要な所だけ各フォルダ詳細を見る。

### `BottomTabs/Common`

- `MainWindow.BottomTabs.Common.cs`
  - 上側メインタブ切替時の共通処理。
  - `SelectFirstItem()`、`Tabs_SelectionChangedAsync()`、選択取得系がある。
- `BottomTabActivationGate.cs`
  - 下部タブが「今更新してよいか」を見る最小ヘルパ。
- `AI向け_Dirkster.AvalonDock省エネ更新の注意点_2026-03-18.md`
  - AvalonDock 下での `dirty`、`IsSelected / IsActive / IsVisible`、初回 measure の注意点を整理した資料。

### `BottomTabs/Extension`

- `MainWindow.BottomTab.Extension.cs`
  - 詳細タブの表示/非表示、選択追従、詳細描画本体。
  - 詳細サムネ生成は、下部詳細タブが前面の時だけ動かす。
- `ExtensionTabPresenter.cs`
  - 活性監視、dirty 管理、可視時 flush の presenter。
- `MainWindow.BottomTab.Extension.DetailThumbnail.cs`
  - 詳細タブ専用サムネの表示モード設定、設定保存、表示時だけ不足分を明示作成要求として通常生成へ優先投入し、ERROR は即時救済へ流す。
  - 表示モード drop-down は `Grid / Small / Big / List / 5x2 / WhiteBrowser 160x90` を選べる。
  - 既定値は `5x2`。旧設定値 `Standard / Big10 / WhiteBrowserCompatible` は現在の選択肢へ正規化して吸収する。
  - 画像枠の表示サイズは、実画像がある時はそのピクセルサイズへ追従し、複数パネル画像も全体を欠けずに見せる。
  - `Grid` は従来の `標準 160x120` と同じ保存先を共有するため、失敗一覧では marker だけで `詳細` を推測しない。
- `ExtensionTabView.xaml`
  - 詳細タブの host。
- `ExtensionTabView.xaml.cs`
  - `ExtDetail` を内包する薄い橋渡し。
 - `UserControls/ExtDetail.xaml`
  - 上段ヘッダーへ `画像サイズ / 動画名 / Dir / 基本メタ情報 / タグ` を1列で並べ、下段は画像を残り領域へ `Uniform` でフィット表示する。

### `BottomTabs/Bookmark`

- `MainWindow.BottomTab.Bookmark.cs`
  - bookmark 読込、再読込、追加、削除、非アクティブ時の defer。
- `BookmarkTabPresenter.cs`
  - 活性監視、dirty 管理、可視時 flush の presenter。
- `BookmarkTabView.xaml`
  - bookmark 一覧 View。
- `BookmarkTabView.xaml.cs`
  - 一覧再描画の窓口。

### `BottomTabs/SavedSearch`

- `MainWindow.BottomTab.SavedSearch.cs`
  - placeholder 文言の管理と defer。
- `SavedSearchTabPresenter.cs`
  - placeholder 文言と dirty 管理の presenter。
- `SavedSearchTabView.xaml`
  - 保存済み検索条件タブの View。
- `SavedSearchTabView.xaml.cs`
  - placeholder 更新の窓口。

### `BottomTabs/ThumbnailProgress`

- `MainWindow.BottomTab.ThumbnailProgress.cs`
  - 進捗 UI の本体。タイマー tick、本体描画、設定 UI、スナップショット反映。
- `ThumbnailProgressTabPresenter.cs`
  - 活性監視、dirty 管理、可視時 flush、タイマー起動停止判断の presenter。
- `ThumbnailProgressTabView.xaml`
  - 進捗タブの View。
- `ThumbnailProgressTabView.xaml.cs`
  - 設定 UI のイベント橋渡し。
- `ThumbnailProgressTabVisibilityGate.cs`
  - 進捗タブ専用の表示判定。
- `Views/Main/MainWindow.xaml`
  - 保存済み `layout.xml` が無い初回起動では、このタブを下部タブの既定選択にする。

### `BottomTabs/LogTab`

- `MainWindow.BottomTab.Log.cs`
  - `Log` タブの表示制御、ログ出力スイッチ保存、ログ末尾プレビュー更新。
  - スイッチは `Properties.Settings` へ即保存し、`DebugRuntimeLog` のカテゴリ判定へ直結する。
- `LogTabPresenter.cs`
  - 活性監視、タイマー起動停止、前面化時 refresh の presenter。
- `LogTabView.xaml`
  - ログ出力先、カテゴリ別チェックボックス、プレビューの View。
- `LogTabView.xaml.cs`
  - ボタンとスイッチ変更のイベント橋渡し。
- `LogTab/Implementation Plan_下部タブ_Logタブ追加_2026-04-11.md`
  - 追加理由、カテゴリ設計、保存方針の実装メモ。

### `BottomTabs/DebugTab`

- `MainWindow.BottomTab.Debug.cs`
  - Debug タブの表示制御、DB/Queue/FailureDB 操作、各種メンテ操作。
  - ログプレビューは持たず、保守操作に役割を絞っている。
- `DebugTabPresenter.cs`
  - 活性監視、タイマー起動停止、前面化時 refresh の presenter。
- `DebugTabView.xaml`
  - Debug タブの View。
- `DebugTabView.xaml.cs`
  - ボタン操作のイベント橋渡し。
- `BottomTabs_Debug切り替え方針_2026-03-25.md`
  - `Debug` / `サムネ失敗Debug` の表示切替フロー（判定、復元、非表示処理）をまとめた運用資料。

### `BottomTabs/ThumbnailError`

- `MainWindow.BottomTab.ThumbnailError.cs`
  - 下部タブ host との接続窓口。
- `MainWindow.BottomTab.ThumbnailError.Progress.cs`
  - サムネ失敗タブ専用の可視監視と 1 秒ポーリング。
  - 見えている間だけ `FailureDb` を軽く再読込し、`待機中` / `救済中` / `反映待ち` を自動追従する。
- `ThumbnailErrorTabPresenter.cs`
  - 活性監視、active cache、タイマー制御の presenter。
- `ThumbnailErrorBottomTabHostController.cs`
  - host 出し入れ、layout 復元時の掃除、DataGrid 取得の controller。
- `ThumbnailErrorTabView.xaml`
  - サムネ失敗一覧、進行状況サマリ、`リストクリア` / 救済ボタンの View。
- `ThumbnailErrorTabView.xaml.cs`
  - ボタンと一覧選択のイベント橋渡し。
- `MainWindow.ThumbnailFailedTab.cs`
  - 実際の救済投入本体。`マーカー + FailureDb 最新親行` を束ねて一覧化し、`リストクリア` は表示中の ERROR マーカーと該当 FailureDb 行を消す。
  - `詳細(99)` は現在の詳細サイズが他タブと保存先共有の時、marker だけでは失敗タブへ加えない。
  - 一覧再読込では重い選択復元は行わず、`選択救済` は `CurrentItem` / `CurrentCell` も見て実行対象を決める。
  - `選択救済`、`一括救済`、`右クリック サムネ救済` は即時 worker 起動要求で流す。

## 現在の設計ルール

- `MainWindow.xaml` は host の殻だけを持つ。
- 各タブ固有の処理は `BottomTabs/<TabName>` へ寄せる。
- 非アクティブ時は更新せず、dirty を立てて、再表示時に 1 回だけ反映する。
- 重い MVVM 化より、追いやすい partial + UserControl を優先する。
- 上側メインタブの index を壊したくない物は、殻だけ残して実体を下部タブへ逃がしてよい。
- 下部タブ資料も、まず `役割サマリ -> 入口 -> 詳細` の順で読める形を保つ。

## 変更時の見方

- 上側タブ切替で何か起きるなら `BottomTabs/Common/MainWindow.BottomTabs.Common.cs`
- 下部タブの表示条件なら対象タブの `MainWindow.BottomTab.*.cs`
- 見た目の変更なら対象タブの `*TabView.xaml`
- 高密度表示を再利用するなら `Themes/Generic.xaml` の `HighDensityBottomTab*` スタイル群
- ボタンや slider の橋渡しなら対象タブの `*TabView.xaml.cs`

## 関連資料

- `Docs/設計メモ_下部タブ分割方針_2026-03-15.md`
- `Docs/Implementation Plan_下部タブ分割_Phase1_サムネ進捗_2026-03-15.md`
