# AI向け レビュー結果 完成形移行初動1回目 2026-03-20

最終更新日: 2026-03-20

## 1. 対象

- T2: Thumbnail.Core handoff 整理
- T3: Watcher `CheckFolderAsync` 薄化

## 2. 結論

- 差分は作成された
- ただし、そのまま受け入れはしない
- 重大 finding が 2 件、中 severity が 1 件あるため、T2/T3 は差し戻しとする

## 3. finding

### F1. 重大

- 内容
  - `visible-only` より先に `WatchScanProcessLimit=200` で切っているため、表示中動画が deferred 側へ押し出される
- 影響
  - `visible-only` の目的である「今見えている動画を先に通す」が崩れる
  - `workthree` の体感テンポ最優先方針に逆行する
- 主な確認箇所
  - `Watcher/MainWindow.Watcher.cs:2096`
  - `Watcher/MainWindow.Watcher.cs:2131`
  - `Watcher/MainWindow.WatchScanCoordinator.cs:355`

### F2. 重大

- 内容
  - deferred watch state が DB 切替を跨いで再混入する経路がある
- 影響
  - 旧 DB の deferred batch が新 DB 側へ流れ込む可能性がある
  - watch 状態機械として危険
- 主な確認箇所
  - `Views/Main/MainWindow.xaml.cs:1012`
  - `Watcher/MainWindow.Watcher.cs:968`
  - `Watcher/MainWindow.Watcher.cs:1043`
  - `Watcher/MainWindow.Watcher.cs:1581`
  - `Watcher/MainWindow.Watcher.cs:1643`
  - `Watcher/MainWindow.Watcher.cs:2100`
  - `Watcher/MainWindow.Watcher.cs:2134`

### F3. 中

- 内容
  - rescue lane 判定が即時設定値ではなく、最大 1 秒古いキャッシュへ変わっている
- 影響
  - 設定変更直後の manual rescue / terminal failure 記録が旧閾値 lane に入る
  - queue 側へ host 設定解決が寄り、完成形の依存方向としても逆向き
- 主な確認箇所
  - `Thumbnail/MainWindow.ThumbnailRescueLane.cs:212`
  - `src/IndigoMovieManager.Thumbnail.Queue/ThumbnailFailureRecorder.cs:91`
  - `src/IndigoMovieManager.Thumbnail.Queue/ThumbnailLaneClassifier.cs:61`
  - `src/IndigoMovieManager.Thumbnail.Queue/ThumbnailLaneClassifier.cs:65`
  - `src/IndigoMovieManager.Thumbnail.Queue/ThumbnailLaneClassifier.cs:73`

## 4. テスト不足

- `visible-only` と deferred batch の組み合わせ順序を保証するテストが不足
- DB 切替中に in-flight scan が `StoreDeferredWatchScanBatch` するケースのテストが不足
- `ThumbnailFailureRecorder` で追加した `HandoffType` / `FailureKind` の `ExtraJson` 契約テストが不足

## 5. 調整役判断

- T1 は受け入れ
- T2 は修正して再レビュー
- T3 は修正して再レビュー
- 次の差し戻し指示では、F1 と F2 を最優先で潰す
