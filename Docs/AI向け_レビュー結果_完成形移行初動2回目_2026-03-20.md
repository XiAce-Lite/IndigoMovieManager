# AI向け レビュー結果 完成形移行初動2回目 2026-03-20

最終更新日: 2026-03-20

## 1. 対象

- T2: Thumbnail.Core handoff 整理 差し戻し後
- T3: Watcher `CheckFolderAsync` 薄化 差し戻し後

## 2. 結論

- T2 は初回 F3 を解消した
- T2 には低 severity のテスト不足が 1 件残るが、blocking ではないため受け入れとする
- T3 は初回 F1 / F2 の修正は入った
- ただし watch を跨いだ deferred backlog 優先の逆流が 1 件残るため、T3 は再差し戻しとする

## 3. finding

### F4. 中

- 対象
  - T3
- 内容
  - 前回の deferred batch が残っていると、`ScanFolderWithStrategyInBackground` が `TryTakeDeferredWatchScanBatch(...)` 成功時点で即 return する
  - そのため、その回の Everything / Filesystem 再収集が走らず、前回 backlog の後に新しく発生した visible 動画でも、deferred を吐き切るまで観測されない
- 影響
  - 単一スキャン内の visible 優先は直っているが、watch を跨ぐと「古い deferred backlog が先、新しい visible が後ろ」の逆流が残る
  - `workthree` の体感テンポ最優先方針にまだ未達
- 主な確認箇所
  - `Watcher/MainWindow.Watcher.cs:2158`

### G1. 低

- 対象
  - T2
- 内容
  - `ResolveThumbnailRescueLaneName(...)` の即時設定読取は確認できる
  - ただし実運用の失敗経路で `ThumbnailQueueBatchRunner` が `handoffLaneResolver` の結果を `ThumbnailFailureRecorder` へ渡すことを固定するテストがまだ無い
- 影響
  - いまの実装は妥当だが、後退検知がやや弱い
- 主な確認箇所
  - `src/IndigoMovieManager.Thumbnail.Queue/ThumbnailQueueBatchRunner.cs:142`
  - `Tests/IndigoMovieManager_fork.Tests/MissingThumbnailRescuePolicyTests.cs:738`
  - `Tests/IndigoMovieManager_fork.Tests/ThumbnailFailureDbTests.cs:1395`

### G2. 低

- 対象
  - T3
- 内容
  - 追加テストは `SplitWatchScanMoviePaths` の単体確認が中心で、`visible-only + 200件上限 + deferred batch` の結合動作をまだ固定できていない
- 影響
  - 今回の残課題 F4 の再発防止が弱い
- 主な確認箇所
  - `Tests/IndigoMovieManager_fork.Tests/WatchVisibleOnlyGatePolicyTests.cs:84`
  - `Tests/IndigoMovieManager_fork.Tests/WatchVisibleOnlyGatePolicyTests.cs:96`
  - `Tests/IndigoMovieManager_fork.Tests/WatchVisibleOnlyGatePolicyTests.cs:112`

## 4. 初回 finding に対する確認結果

- 初回 F3
  - 解消
  - host の即時設定値で lane を決める形へ戻った
- 初回 F1
  - 単一スキャン内では解消
  - ただし watch またぎの deferred backlog 優先という別形の逆流が残った
- 初回 F2
  - 解消
  - deferred state / last_sync は DB スコープ付きへ寄った

## 5. 調整役判断

- T2 は受け入れ
- T2 の G1 は後追いテスト補強 ToDo として残す
- T3 は F4 を潰して再レビュー
- T3 は G2 を同時に埋める

## 6. 次アクション

1. T3 を F4 観点で再差し戻しする
2. T3 で deferred backlog と新規 visible 候補の優先関係を再設計する
3. T3 に結合テストを追加してからレビュー専任へ再投入する
