# AI向け レビュー結果 完成形移行初動3回目 2026-03-20

最終更新日: 2026-03-20

## 1. 対象

- T3: Watcher `CheckFolderAsync` 薄化 再差し戻し後

## 2. 結論

- 再差し戻しで指摘した F4 は解消した
- blocking な finding は無い
- 残留リスクはテスト粒度に関する低リスクだけなので、T3 は受け入れとする

## 3. レビュー結果

- findingsなし

## 4. 残留リスク

- `FinalizeWatchScanWithDeferredCandidates(...)` を通した結合確認はまだ薄い
- 特に `Everything` 経路で、`last_sync` が「deferred 解消後だけ保存される」ことを直接押さえるテストは未追加
- 「旧 DB に deferred が残ったまま DB 切替」の実運用寄り確認も、helper 単位ではなく呼び出し経路込みでは未実施

## 5. 主な確認箇所

- `Watcher/MainWindow.Watcher.cs:2156`
- `Watcher/MainWindow.Watcher.cs:2311`
- `Watcher/MainWindow.Watcher.cs:1702`
- `Watcher/MainWindow.Watcher.cs:1950`
- `Watcher/MainWindow.Watcher.cs:1995`
- `Tests/IndigoMovieManager_fork.Tests/WatchVisibleOnlyGatePolicyTests.cs:106`
- `Tests/IndigoMovieManager_fork.Tests/WatchVisibleOnlyGatePolicyTests.cs:135`
- `Tests/IndigoMovieManager_fork.Tests/WatchDeferredScanStatePolicyTests.cs:7`

## 6. 調整役判断

- T3 は受け入れ
- 初動 3 タスクは全て受け入れ完了
- 残留リスクは後続の補強テスト / 実運用寄り確認タスクへ回す

## 7. 次アクション

1. T1 の棚卸し結果を使い、Data 入口集約の実装タスクを起票する
2. T2 の `handoffLaneResolver` 経路テスト補強を後続タスク化する
3. T3 の `Everything + last_sync + deferred` 結合確認を後続タスク化する
