# 仕分け結果 future 4コミット workthree 取り込み判定 2026-03-11

最終更新日: 2026-03-11

## 1. 目的

`future` 側で積まれた 4 コミットを、`workthree` へ持ってよいかを `安全 / 注意 / 危険` で判定する。

## 2. 判定表

| コミット | 要約 | 判定 | 理由 | 推奨対応 |
|---|---|---|---|---|
| `f502946` | AI向けブランチ方針文書と導線を追加 | 安全 | ドキュメント導線だけで、実装挙動へ触れない | `workthree` 用に文面を差し替えて手動反映 |
| `6db7e3b` | 当面の目標をAI向け方針文書に反映 | 注意 | そのまま持つと `future` の優先順位を `workthree` へ誤転写する | 構造だけ流用し、目標は `workthree` 用に再定義 |
| `242af8e` | UI一覧更新をFilteredMovieRecsへ一本化 | 危険 | メイン一覧更新経路の中心を変える。`workthree` の難読動画検証とは別軸で、回帰範囲が広い | 今は持ち込まない。必要なら別枝で単独検証 |
| `8fdab21` | UI一覧再読込を非同期化して巻き戻りを防止 | 危険 | 非同期化と revision 管理が入り、表示更新の順序保証が変わる。`workthree` で混ぜる価値よりリスクが高い | 今は持ち込まない。`future` 本線で十分に枯らしてから再検討 |

## 3. 結論

- すぐ持ってよいのは、文書導線の考え方だけである。
- `future` の UI 実装 2 コミットは、`workthree` の主目標とズレるため現時点では持ち込まない。
- `workthree` で必要なのは、難読動画検証に効く観測性、比較テスト、一般条件の短文化である。
