# インストーラー計画_完了確認メモ_2026-04-06

最終更新日: 2026-04-06

## 1. 先に結論

2026-04-06 時点で、インストーラー計画は次の状態まで到達した。

1. `WiX v6 + Burn bundle` の build 導線は生きている
2. `.NET 8 Desktop Runtime` prerequisite を bundle 側へ入れた
3. GitHub Actions preview で installer artifact 生成が成功した
4. tag release で `ZIP + installer exe` が GitHub Release asset に並ぶことを確認した

ただし、**まだ完了扱いにはしない**。
残っているのは、実機に近い install / upgrade proof である。

## 2. 今回確認できた事実

### 2.1 prerequisite 実装

- commit: `ce9021d`
- 変更点:
  - `installer/wix/Bundle.wxs`
  - `installer/wix/IndigoMovieManager.Bundle.wixproj`
  - `scripts/create_wix_installer_from_release_package.ps1`
- 内容:
  - bundle 起動時に `.NET 8 Desktop Runtime x64` を検知する
  - 未導入なら Microsoft 公式配布元から prerequisite を取得できる形へした

### 2.2 preview proof

- GitHub Actions run: `24013526570`
- URL:
  - https://github.com/T-Hamada0101/IndigoMovieManager_fork/actions/runs/24013526570
- 結果:
  - `success`
- 確認できたこと:
  - `Create WiX installer` が成功
  - `github-release-installer` artifact が出る

### 2.3 tag release proof

- tag: `v0.0.0-local`
- GitHub Actions run: `24013760452`
- URL:
  - https://github.com/T-Hamada0101/IndigoMovieManager_fork/actions/runs/24013760452
- 結果:
  - `success`
- GitHub Release:
  - https://github.com/T-Hamada0101/IndigoMovieManager_fork/releases/tag/v0.0.0-local
- 確認できた asset:
  - `IndigoMovieManager-Setup-v0.0.0-local-win-x64.exe`
  - `IndigoMovieManager-v0.0.0-local-win-x64.zip`

## 3. まだ未了のもの

以下が終わるまでは、インストーラー計画を「完了」とは言わない。

1. クリーン環境 install proof
2. 旧版あり環境からの upgrade proof

## 4. 今の判断

- `インストーラー計画は大きく前進した`
- `release workflow 連携も prerequisite 導線も通った`
- ただし、**最終完了条件はまだ満たしていない**

よって、現時点の表現は次が正しい。

- `インストーラー計画は本番導線まで到達した`
- `残件は install / upgrade の live proof である`
