# 実測調整メモ: サムネイル高負荷係数と閾値（2026-03-07）

## 1. 目的
- `LTH-024` として、高負荷係数と閾値を実運用ログベースで見直す。
- ただし 2026-03-07 時点では、現行形式の `thumb queue summary` ログがまだ採取できていないため、まずは「再調整可能な状態」と「暫定既定値」を固定する。

## 2. 採取結果
- 確認日時:
  - 2026-03-07
- 確認ファイル:
  - `%LOCALAPPDATA%\\IndigoMovieManager_fork\\logs\\debug-runtime.log`
- 確認結果:
  - 最新の既存ログは 2026-02-24 22:26 台で、`thumb queue summary:` 行は未出力だった。
  - つまり、`LTH-020` 以降の `high_load`, `thermal_score`, `usnmft_score`, `admin_fallback_kind` を含む現行summary形式では、まだ実機データが揃っていない。

## 3. 今回の判断
- 実機summary未採取を理由に再調整作業を止めるより、係数と閾値を `Settings` へ外出しして次回以降の無改修調整を可能にする方を優先する。
- 既定値は「過剰縮退を少し減らす」方向へ暫定調整する。
  - `QueuePressure` を 0.30 -> 0.25
  - `SlowBacklog` を 0.15 -> 0.10
  - `RecoveryBacklog` を 0.15 -> 0.10
  - `UsnMftBusy` を 0.15 -> 0.10
  - `RecoveryThreshold` を 0.45 -> 0.48
  - `MildThreshold` を 0.55 -> 0.60
  - `HighThreshold` を 0.75 -> 0.82
  - `DangerThreshold` を 0.90 -> 0.95
- `Error` と `Thermal` は安全側を優先して据え置く。

## 4. 設定化した項目
- `ThumbnailParallelHighLoadWeightError`
- `ThumbnailParallelHighLoadWeightQueuePressure`
- `ThumbnailParallelHighLoadWeightSlowBacklog`
- `ThumbnailParallelHighLoadWeightRecoveryBacklog`
- `ThumbnailParallelHighLoadWeightThroughputPenalty`
- `ThumbnailParallelHighLoadWeightThermalWarning`
- `ThumbnailParallelHighLoadWeightUsnMftBusy`
- `ThumbnailParallelHighLoadRecoveryThreshold`
- `ThumbnailParallelHighLoadMildThreshold`
- `ThumbnailParallelHighLoadThreshold`
- `ThumbnailParallelHighLoadDangerThreshold`

## 5. 次回の実測手順
1. `ManualRegressionCheck_サムネイルスレッド制御とレーン設計_2026-03-06.md` の `fast` / 高負荷縮退シナリオを流す。
2. `debug-runtime.log` から `thumb queue summary:` 行を採取する。
3. `high_load`, `queue_score`, `throughput_score`, `thermal_score`, `usnmft_score`, `parallel_next` を見て、過剰縮退の有無を判断する。
4. 必要なら今回追加した `Settings` 値だけを変更し、再度同条件で比較する。

## 6. 現時点の結論
- 2026-03-07 時点の実運用ログでは現行summary形式の観測値が未取得だった。
- そのため今回の `LTH-024` は「暫定既定値の見直し」と「次回実測を無改修で回せる設定化」を完了条件とする。
