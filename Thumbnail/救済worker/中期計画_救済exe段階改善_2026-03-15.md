# 中期計画 救済exe段階改善 2026-03-15

最終更新日: 2026-03-15

変更概要:
- `MID-008` の到達点として、`古い.wmv` は forced repair 経路で攻略済みになった
- 決定打は `repair_probe_negative` 後の forced repair、WMV/ASF の `video-only retry`、non-monotonic DTS packet skip、repair 後の engine 順保持である
- 次の主戦場は `opencv timeout` 後の出口設計ではなく、`shiroka8.mp4` の `repair_probe_negative` 終端である
 - `p5` の追補として、`repair_probe_negative` fallback で明示した残り engine 順を route 昇格後も維持するよう修正した
- `古い.wmv` の parent worker live で、`route-long-no-frames -> route-corrupt-or-partial -> autogen -> opencv(timeout)` まで到達することを確認した
- `p5` の後段として、`opencv` を rescue worker 本体と別の子プロセスへ隔離し、timeout 超過時に親 worker から kill できる形を入れた
- `ShouldUseIsolatedChildProcess("opencv")` と child 引数組立/復元の単体テストを追加した
- `p5 / p6` の観測性強化として、`IMM_THUMB_RESCUE_TRACE=1` でだけ有効になる専用 CSV を追加した
- 出力先は `%LOCALAPPDATA%\\IndigoMovieManager_fork_workthree\\logs\\thumbnail-rescue-trace.csv` とし、main / worker のイベントを共通列で追えるようにした
- 記録列は `movie_path / tab_index / panel_size / route / phase / engine / action / result / reason / output_path` を基本とする
- `p6` の入口として、`gave_up / 長期 processing_rescue / legacy route 欠損` を分けて見るサマリを追加した
- `p6` の初回レポートで、主課題が `route-long-no-frames` の `repair_probe_negative` と stale processing 整理へ寄ることを確認した
- `p5` live で、`ffmpeg one-pass failed` が `TransientDecodeFailure`、`frame decode failed at sec=...` が `IndexCorruption` として入ることを確認した
- 確認は `C:\WhiteBrowser\X.wb` に対する手動 worker 実行で行い、`failure_id=3 (shiroka8.mp4)` が `ffmpeg1pass -> ffmediatoolkit -> repair_probe_negative -> gave_up` を通った
- `p5` live で、`engine attempt timeout ...` が `attempt_failed.kind=HangSuspected` として FailureDb へ入ることを確認した
- 確認個体は `C:\WhiteBrowser\難読.wb` の `failure_id=4` で、`ffmpeg1pass` 120 秒 timeout 後に `route-long-no-frames` へ昇格している
- `p5` の観測性強化として、`attempt_failed.kind` の `Unknown` を減らす failure kind 寄せを追加した
- `frame decode failed ...` を `IndexCorruption`、`ffmpeg one-pass failed` を `TransientDecodeFailure`、timeout 文言を `HangSuspected` へ寄せるようにした
- `p5` live で、`tab-error-placeholder` 起点の `fixed / unclassified` 個体が `route-long-no-frames` へ途中昇格することを確認した
- `p5` の追補として、親 failure reason が弱い `fixed / unclassified` 個体を、最初の direct failure から既存 route へ途中昇格できるようにした
- `p4 -> p5` の方針として、新 route 追加より先に既存 route の分類語彙と repair gate を強化する方針を明記した
- `long-no-frames` へ timeout 系と `ffmpeg one-pass failed` 系を寄せる初手を実装した
- `repair` は route-aware gate へ上げ、`long-no-frames` と `corrupt-or-partial` を優先して repair へ入れる形にした
- `p4` の入口として、失敗束サマリを `RouteId / SymptomClass` 付きで読めるようにした
- `near-black` と `fixed fallback` の単体テストを追加し、route 化後の誤分類洗い出しを始めた
- 現物の代表束はまだ `fixed / unclassified` が多いことを確認した
- `p3` の初手として、`ClassifyRescueSymptom(...)` と `BuildRescuePlan(...)` を実装した
- `long-no-frames / ultra-short-no-frames / near-black-or-old-frame / corrupt-or-partial / fixed` の route 骨格を入れた
- 親行 `ExtraJson` と子行 `attempt_failed` に `RouteId / SymptomClass` を載せるようにした
- 救済exeを fixed 順から症状分岐型へ上げるための中期計画を追加した
- 各段で「アルゴ更新」と「テストで失敗洗い出し」を交互に回す進め方を固定した
- `p6 -> p5` の反復条件を明文化した
- `p1` の初手として、fixed 順でも親行 `ExtraJson` に `RouteId=fixed / CurrentPhase / CurrentEngine` を書く方針を反映した
- `p2` の入口として、`attempt_failed` を engine / kind / reason で束ねる失敗洗い出しスクリプトを追加した
- `MID-009` の前半として、`out1.avi` は forced repair + AVI remux 補強で攻略済みになった
- 決定打は `route-ultra-short-no-frames` から `route-corrupt-or-partial` への途中昇格、AVI forced repair、`video-only retry`、missing timestamp 補完/skip である
- isolated live で repaired source の `opencv` 成功と `rescued` 到達を確認した
- `MID-009` の後段として、`shiroka8.mp4` も autogen の `header-fallback` 相当救済で攻略済みになった
- 決定打は、autogen へ `No frames decoded` 時の先頭候補再探索とタイル複製を最小移植したことである
- 同じ実装で `na04.mp4` も isolated live で攻略済みになった
- `35967.mp4` と `みずがめ座 (2).mp4` は現行 worker で `ffmpeg1pass.direct` 勝ちを確認した
- `画像1枚ありページ.mkv` と `画像1枚あり顔.mkv` は `route-ultra-short-no-frames -> ffmpeg1pass.direct` 勝ちを確認した
- `「ラ・ラ・ランド」は少女漫画か！？ 1_2.mp4` と `2_2.mp4` は `probe_negative_fallback -> autogen` 勝ちを確認した
- `真空エラー2_ghq5_temp.mp4` は `route-long-no-frames -> ffmpeg1pass.direct` 勝ちを確認した
- `mpcクラッシュ_NGカット.tmp.flv` は `route-ultra-short-no-frames -> autogen.direct` 勝ちを確認した
- `映像なし_scale_2x_prob-3(1)_scale_2x_prob-3.mkv` は `video_present` だが `ffplay` でも `unspecified pixel format` で表示できず、既存 `CODEC NG` 固定へ見直した
- `【ライブ配信】神回scale_2x_prob-3.mp4` は `tab-error-placeholder` 起点の `fixed / unclassified -> ffmediatoolkit.direct` 勝ちを確認した
- `_steph__094110-vid1.mp4` は `tab-error-placeholder` 起点の `fixed / unclassified -> ffmpeg1pass.direct` 勝ちを確認した
- `インデックス破壊-093-2-4K.mp4` は `route-long-no-frames -> route-corrupt-or-partial -> probe_negative_fallback -> autogen` 勝ちを確認した
- `mpcクラッシュ_再生できない.flv` は `route-long-no-frames -> ffmpeg1pass.direct` 勝ちを確認した
- `インデックス破壊-093-2-4K.remux.mp4` は `tab-error-placeholder` 起点の `fixed / unclassified -> ffmpeg1pass.direct` 勝ちを確認した
- 同じ `インデックス破壊-093-2-4K.remux.mp4` で `detail-selection-error-placeholder` も `fixed / unclassified -> ffmpeg1pass.direct` 勝ちを確認した
- stale lease 回収を `FailureDb` へ追加し、sync / manual enqueue / worker launch 前で `processing_rescue -> pending_rescue` を回収するようにした
- 実 DB `難読.09FDC56B.failure.imm` でも `failure_id=3,18` を `pending_rescue` へ回収し、`processing_rescue=0` を確認した
- 回収した `failure_id=3,18` は isolated live でも
  `ffmpeg1pass timeout -> route-long-no-frames -> ffmediatoolkit.direct`
  で `rescued` まで確認した
- よって `MID-010` は「長尺/短尺 No frames decoded 束の横展開確認」として完了扱いに上げる
- 次の主戦場は stale placeholder の再発防止と、まだ isolated 未確認の pending 個体整理である

## 1. 目的

- 本体は `autogen` 単発で早期見切りまで到達した
- 残る主戦場は救済exeである
- ただし救済exeを一気に作り替えると、何が効いたかと何が壊れたかが追いにくい
- そのため中期計画は、次の 6 段で進める

## 2. 基本方針

### 2.1 先に順序を固め、その後で分岐へ上げる

- いきなり症状分岐型へ飛ばない
- まず fixed 順の各処理を詰めて、基準線を安定させる
- その後で症状分岐型へ上げる

### 2.2 各段の直後に必ず失敗洗い出しを入れる

- アルゴ更新だけを連続で積まない
- 各段の直後にテストと実測で失敗を洗い出す
- そこで見えた失敗を次段へ返す

### 2.3 本体へ重さを戻さない

- 改善対象は救済exe側に閉じる
- 本体の `autogen` 単発方針は維持する

## 3. フェーズ一覧

| Phase | 内容 | 目的 | 主成果物 |
| --- | --- | --- | --- |
| `p1` | 救済exeの fixed 順各処理のアルゴ更新 | 現行基準線を強くする | engine 別アルゴ更新、ログ整理、補助テスト |
| `p2` | テストで失敗洗い出し | fixed 順の穴を見つける | 失敗一覧、再現条件、追加テスト |
| `p3` | fixed 順から症状分岐型 | route 化の骨格を入れる | `SymptomClass`, `RouteId`, route 選択 |
| `p4` | テストで失敗洗い出し | route 化の穴を見つける | route 別失敗一覧、誤分類一覧 |
| `p5` | 症状分岐型各処理のアルゴ更新 | route ごとの成功率を上げる | route 別アルゴ更新、追加ログ、補助指標 |
| `p6` | テストで失敗洗い出し | route ごとの未解決を絞る | 未解決束、次の `p5` 改善材料 |

## 4. 各フェーズの中身

### 4.1 `p1` 救済exeの fixed 順各処理のアルゴ更新

対象:
- `ffmpeg1pass`
- `ffmediatoolkit`
- `autogen`
- `opencv`
- `repair.probe`
- `repair.remux`

ここでやること:
- engine ごとの timeout 方針見直し
- `No frames decoded` 周りの文言吸収
- `repair gate` 語彙の拡張
- `FailureDb` 試行ログの質向上
- `CurrentEngine` / `CurrentPhase` の観測性整理

完了条件:
- fixed 順のままでも、各 engine の失敗理由が比較可能
- 既知の timeout / repair 入口 / 出力欠損が観測で追える

### 4.2 `p2` テストで失敗洗い出し

ここでやること:
- 単体テストで engine ごとの失敗を洗う
- 実動画で代表ケースを再投入する
- `FailureDb` とログから失敗束を作る
- `救済worker失敗束サマリ_2026-03-15.ps1` を使って、`attempt_failed` の偏りを先に掴む

見る観点:
- timeout 不達
- `gave_up` が早すぎる
- `repair` へ入るべき個体が漏れる
- `opencv` の居座り
  - ただし `opencv` は子プロセス隔離を入れたので、次の観測点は live kill 後の継続性である
- `near-black` と `no-frame` の混同

成果物:
- 失敗一覧
- 再現個体
- 追加するべき分類キー

### 4.3 `p3` fixed 順から症状分岐型

ここでやること:
- `ClassifyRescueSymptom(...)` を追加する
- `BuildRescuePlan(...)` を route 別に持つ
- parent 行 `ExtraJson` に
  - `SymptomClass`
  - `RouteId`
  - `CurrentEngine`
  - `CurrentPhase`
  を載せる

初版 route:
- `long-no-frames`
- `ultra-short-no-frames`
- `near-black-or-old-frame`
- `corrupt-or-partial`

完了条件:
- fixed 順を直書きせず、route から engine 列が決まる
- 親行だけ見て「今どの route で止まっているか」が分かる

### 4.4 `p4` テストで失敗洗い出し

ここでやること:
- route ごとの単体テストを足す
- 誤分類ケースを洗う
- route には入ったが順序が悪いケースを洗う

見る観点:
- `ultra-short` と `long` の誤分類
- `corrupt-or-partial` の過検出
- `near-black` の取りこぼし
- route によって成功率が下がったケース
- 新 route を足す前に、既存 route へ寄せられる束が残っていないか

成果物:
- 誤分類一覧
- route 順序の再調整候補
- 追加すべき軽量判定項目

### 4.5 `p5` 症状分岐型各処理のアルゴ更新

ここでやること:
- 新 route 追加より先に、既存 route の分類語彙を強める
- 新 route 追加より先に、repair gate を route ごとに強める
- route ごとに engine 順を詰める
- `repair` を使う条件を route ごとに絞る
- `opencv` を route ごとに使うかどうかを見直す
- `FailureDb` への progress snapshot を整理する
- `IMM_THUMB_RESCUE_TRACE=1` 時だけ、改善処理専用 CSV で route / phase / engine / terminal を追えるようにする

主対象:
- `R-A long-no-frames`
- `R-B ultra-short-no-frames`
- `R-C near-black-or-old-frame`
- `R-D corrupt-or-partial`

完了条件:
- route ごとに「先に軽い手、後ろに重い手」が守られている
- 失敗時に次の一手が reason から読める
- `attempt_failed.kind` が `Unknown` に寄りすぎず、FailureDb 束だけで主因が読める
- `fixed / unclassified` 束が、語彙追加だけで寄せられる範囲まで減っている

### 4.6 `p6` テストで失敗洗い出し

ここでやること:
- route ごとの代表ケースを再実測する
- 未解決束を作る
- 未解決を `p5` のどの route へ返すか整理する

見る観点:
- まだ `gave_up` へ落ちる個体
- route 末尾まで使っても取れない個体
- watchdog が必要な個体
- 新しい分類が必要な個体

成果物:
- route 別未解決束
- `p5` へ戻す改善課題

## 5. 反復ルール

### 5.1 基本ループ

1. `p1`
2. `p2`
3. `p3`
4. `p4`
5. `p5`
6. `p6`
7. `p6` の結果を持って `p5` へ戻る

### 5.2 `p6 -> p5` へ戻る条件

- route の分類は正しいが、順序や gate が弱い
- `repair` の前後関係が悪い
- `opencv` を使うべき route と不要な route が見えてきた
- `near-black` / `old-frame` / `no-frame` がまだ粗い

### 5.3 `p6` で止めてよい条件

- 新しい未解決が watchdog 系だけに絞れた
- route 追加より engine hard timeout の方が先と判断できた
- 代表ケースの成功率が十分に上がった

## 6. 直近の着手順

1. `p1`
   - fixed 順のまま `CurrentEngine` / `CurrentPhase` / `RouteId=fixed` を整理する
   - `opencv` nominal timeout 分離を入れる
2. `p2`
   - fixed 順で落ちる代表ケースを洗い出す
3. `p3`
   - `ClassifyRescueSymptom(...)`
   - `BuildRescuePlan(...)`
   を入れる
4. `p4`
   - route ごとの誤分類と失敗束を洗う
5. `p5`
   - 新 route 追加より先に、既存 route の分類語彙と repair gate を強化する
6. `p6`
   - `救済worker未解決束サマリ_2026-03-15.ps1` で unresolved を route 別に切る
   - `救済worker\未解決束レポート_p6_2026-03-15.md` に `p5` へ返す課題を固定する
7. live
   - `古い.wmv` で `opencv` 子プロセス timeout 後に親 worker が継続することを確認する
   - 完了。さらに forced repair -> repaired `ffmediatoolkit` 成功まで確認済み
   - `shiroka8.mp4` も autogen の `header-fallback` 相当救済で攻略済み
   - 次は `na04.mp4`、`35967.mp4`、`みずがめ座 (2).mp4` へ同系統を横展開する

## 7. タスクリスト

| ID | 状態 | フェーズ | タスク |
| --- | --- | --- | --- |
| `MID-001` | 完了 | `p1` | fixed 順各処理のアルゴ更新を進める |
| `MID-001a` | 完了 | `p1` | 親行 progress snapshot を追加する |
| `MID-002` | 着手中 | `p2` | fixed 順の失敗洗い出しをテストへ落とす |
| `MID-002a` | 完了 | `p2` | `attempt_failed` 束サマリスクリプトを追加する |
| `MID-003` | 完了 | `p3` | 症状分岐型の route 骨格を入れる |
| `MID-004` | 着手中 | `p4` | route 化後の誤分類と失敗束を洗う |
| `MID-005` | 着手中 | `p5` | route ごとのアルゴ更新を進める |
| `MID-006` | 着手中 | `p6` | 未解決束を作り、次の `p5` へ返す |
| `MID-007` | 完了 | `p5` | `opencv` 子プロセス hard timeout の live 実測を取る |
| `MID-008` | 完了 | `p5` | `古い.wmv` の forced repair 経路を詰め、repair 後 `ffmediatoolkit` 成功まで確認する |
| `MID-009` | 完了 | `p5` | `shiroka8.mp4` の `repair_probe_negative` 終端を success 側へ寄せる |
| `MID-010` | 完了 | `p5` | 長尺/短尺 `No frames decoded` 束の横展開を `35967.mp4` / `みずがめ座 (2).mp4` / `画像1枚あり*` / `ラ・ラ・ランド*` まで確認する |
| `MID-011` | 完了 | `p5` | `mpcクラッシュ_NGカット.tmp.flv` の FLV 系勝ち筋を切る |
| `MID-012` | 完了 | `p5` | `映像なし_scale_2x_prob-3(1)_scale_2x_prob-3.mkv` の timeout 混在束を切る |

## 8. 一言まとめ

- まず fixed 順を詰める
- 次に症状分岐型へ上げる
- `古い.wmv` は forced repair 経路で攻略済み
- `out1.avi` も forced repair + AVI remux 補強で攻略済み
- その後は `p5 <-> p6` を回して、残る timeout 混在系 / placeholder 系 / reuse 系の未解決束へ進む
- placeholder 系の direct 勝ち筋棚卸しと `IndexCorruption` 近縁 remux は一段付いたため、次は stale placeholder 束を詰める
