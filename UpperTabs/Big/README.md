# UpperTabs Big

このフォルダは、上側 `Big` タブの個別 dir 化を進めるための入口です。

## 今回の段階

- `MainWindow.UpperTab.Big.cs` を追加
- `Big` タブを既定表示へ戻す入口
- `BigList` 取得と先頭選択

## 次に寄せる候補

- `Big` タブ固有の選択同期
- `Big` タブ固有の viewport / visible-first 補助
- 将来の `BigTabView` host 化
