# UpperTabs Grid

このフォルダは、上側 `Grid` タブの個別 dir 化を進めるための入口です。

## 今回の段階

- `MainWindow.UpperTab.Grid.cs` を追加
- `Grid` タブを既定表示へ戻す入口
- `GridList` 取得と先頭選択

## 次に寄せる候補

- `Grid` タブ固有の選択同期
- `Grid` タブ固有の viewport / visible-first 補助
- 将来の `GridTabView` host 化
