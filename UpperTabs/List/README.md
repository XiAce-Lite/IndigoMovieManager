# UpperTabs List

このフォルダは、上側 `List` タブの個別 dir 化を進めるための入口です。

## 今回の段階

- `MainWindow.UpperTab.List.cs` を追加
- `List` タブを既定表示へ戻す入口
- `ListDataGrid` 取得と先頭選択

## 次に寄せる候補

- `List` タブ固有の選択同期
- `List` タブ固有のキー操作とフォーカス制御
- 将来の `ListTabView` host 化
