# UpperTabs Small

このフォルダは、上側 `Small` タブの個別 dir 化を進めるための入口です。

## 今回の段階

- `MainWindow.UpperTab.Small.cs` を追加
- `Small` タブを既定表示へ戻す入口
- `SmallList` 取得と先頭選択

## 次に寄せる候補

- `Small` タブ固有の選択同期
- `Small` タブ固有の viewport / visible-first 補助
- 将来の `SmallTabView` host 化
