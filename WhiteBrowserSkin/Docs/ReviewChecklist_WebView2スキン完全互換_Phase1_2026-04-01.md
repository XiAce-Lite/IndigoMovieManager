# レビュー観点: WebView2 スキン完全互換 Phase 1 2026-04-01

## 1. 設計
- 二刀流方針が守られているか
- `Phase 0` の 5 決定を破っていないか
- `MainWindow` に責務を戻していないか
- WebView2 依存を Host / Runtime / Orchestrator に閉じ込められているか

## 2. 互換
- `wblib.js` を自前互換で差し替える前提が守られているか
- Shift_JIS HTML の UTF-8 正規化経路があるか
- `skin.local` / `thum.local` の方針が崩れていないか
- `file:///` 直参照が入っていないか

## 3. UI / 体感
- 既存 WPF タブの表示経路を壊していないか
- Runtime 未導入時に安全フォールバックするか
- スキン切替 / DB 切替で残像が出ないか

## 4. セキュリティ
- 既定コンテキストメニューが抑制されているか
- 外部 URL ナビゲーションをブロックできる形か
- 危険 API を先行実装していないか

## 5. テスト
- 純粋ロジックが WebView2 実体なしで検証できる形か
- 文字コード正規化のテストが書ける形か
- Host 依存がテスト不能な形で直結していないか
